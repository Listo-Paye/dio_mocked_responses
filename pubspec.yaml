name: dio_mocked_responses
description: Interceptor that help you to mock backend responses in flutter project.
version: 1.6.1
homepage: https://listo.pro
repository: https://github.com/Listo-Paye/dio_mocked_responses

environment:
  sdk: '>=3.3.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0
  template_expressions: ^3.3.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
