name: dio_mocked_response_example
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.8
  get_it: ^8.0.3
  injectable: ^2.5.0
  dio_cache_interceptor: ^3.5.1
  dio_cache_interceptor_hive_store: ^4.0.0
  hive: ^2.2.3
  freezed_annotation: ^2.4.4
  go_router: ^14.7.1
  dio_oidc_interceptor: ^1.2.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.14
  injectable_generator: ^2.6.2
  retrofit_generator: ^9.1.7
  json_serializable: ^6.9.0
  bdd_widget_test: ^1.7.6
  freezed: ^2.5.7
  dio_mocked_responses:
    path: ../

flutter:
  uses-material-design: true
